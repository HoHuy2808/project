create or replace TABLE DATA_LAKE.TEST_PUBLIC_HH.ANALYST (
	ID VARCHAR(16777216) NOT NULL,
	WEEK VARCHAR(16777216) NOT NULL,
	RANK_CHANGE_PER_WEEK NUMBER(38,0),
	WEEKLY_REVENUE VARCHAR(16777216),
	GROSS_CHANGE_PER_WEEK VARCHAR(16777216),
	RATING FLOAT,
	TITLE VARCHAR(16777216),
	primary key (ID, WEEK)
);